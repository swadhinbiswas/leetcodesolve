class Solve:
    def countZero(row: int, column: int):
        
        count_list = []
        for i in range(column):
            numbers = [int(x) for x in input().split()][:row]
            numbers.count(0)





